services:
  jekyll:
    image: jekyll/jekyll:4.2.0
    command: jekyll serve --livereload --force_polling
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
    environment:
      - JEKYLL_ENV=development
      - JEKYLL_UID=1001
      - JEKYLL_GID=1001
